(function(){
'use strict';
angular.module('kharchApp').factory('HomeService',['$http', '$q', 'urls', homeService]);

    function homeService($http, $q, urls) {
    	
    	return {
    		
    	};
    }
 })();   